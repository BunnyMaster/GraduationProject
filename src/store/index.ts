import { createStore } from "vuex";
import MenuBar from "@/store/modules/MenuBar";
export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: { MenuBar },
});
